# dependencies
/node_modules

# next.js
/.next/
*.tsbuildinfo

# env files (SECURITY: never commit secrets — API keys live here)
.env
.env.*
!.env.example

# runtime download artifacts (binary clips)
/.data/clips/

# os / editor / local tooling
.DS_Store
/.omx/
/.playwright-mcp/

# working screenshots & archive (not source)
/*.png
/shorts-intelligence-dashboard.zip
