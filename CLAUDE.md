# Shorts Intelligence Dashboard — Project Rules

## North Star (모든 결정의 기준점)
사용자가 대시보드를 **열면 아무 조작 없이**, 기간(지금/이번주/이번달)별로 **신뢰할 수 있는 '터진 영상'**이 이미 차 있고, 각 영상이 **왜 터졌는지(구독 대비 배율·기간·등급)를 스스로 말한다.** 수집은 배경에서 자동으로 돌고 화면 전면에 나서지 않는다.

## 제품 목적
'터진 영상'(절대 조회수가 아니라 **구독자 대비 배율·신선 속도**로 폭발한 YouTube Shorts)을 신뢰성 있게 발견해 벤치마킹하는 리서치 대시보드. pint.kr이 벤치마크. (단순 유튜브 검색기가 아님)

## UX 3원칙 (모든 UI 결정은 이 셋을 통과해야 함)
1. **심플** — 설명이 없어도 사용하기 쉬울 만큼 인터페이스가 단순하다. 학습 곡선 0을 목표로. 처음 보는 사람이 헤매면 실패.
2. **미니멀 기능** — 군더더기 없이 딱 필요한 기능만. 모든 기능은 North Star 기여로 정당화한다(YAGNI). "있으면 좋은" 기능·옵션·화면 금지. 기능을 더하기 전에 뺄 수 있는지 먼저 본다.
3. **발견성 + 신뢰** — 원하는 주제의 영상을 찾기 쉽고, 화면의 모든 데이터에 믿음이 간다. 실측은 실측으로, 추정/비공개는 명확히 표기. 가짜 숫자 0.

## UI 카피 언어 원칙
- 핵심 용어·라벨·브랜드는 **영어 유지 가능** (Dashboard, Rankings, Breakout 등 — 간결·세련된 톤).
- 단 전체 톤은 **한글 친화적**으로. 무분별한 영어 남발 금지. 설명·안내·상태·버튼 문구는 한국어로 자연스럽게.
- 사용자는 한국어 화자다. 영어 전문용어가 이해를 막으면 한국어로 바꾼다. (예: "velocity" 단독 노출보다 "시간당 조회수")

## 기술 스택 / 검증
- Next.js App Router + TypeScript + React + lucide-react. 데이터: `.data/shorts-iq.json` (파일 기반, atomic write).
- dev 서버: `http://127.0.0.1:8792` (사용자 실행 중 — **종료 금지**). pnpm: `export PATH="$HOME/.nvm/versions/node/v22.21.1/bin:$PATH"`.
- **검증 필수**: `tsc --noEmit` + 브라우저 시각 확인. 빌드 통과 ≠ 동작. 시각 확인은 `node scripts/visual-verify.mjs`(8792에 read-only 접속, 스크린샷+콘솔에러).
- UI 변경 후 dev 서버 + 브라우저로 직접 확인 전엔 "완료" 선언 금지.

## 정합성 (기획=설계=구현 표류 방지)
- 상세: `~/.claude/rules/alignment.md`. 작업 시작 시 North Star 고정 → 단계마다 "North Star에 기여하나" 자문 → 표류 시 즉시 정지·재정렬 → 완료 전 기획↔구현 역대조. 검수는 구현과 다른 컨텍스트에서.

## 자동 수집
- 앱 진입 시 데이터가 stale(빈 상태 또는 마지막 수집 N시간 경과)이면 **자동으로 discover 1회**(논블로킹). 외부 데몬(`scripts/collector.mjs`)과 별개로, 앱 자체가 "열면 차있음"을 보장한다. 자동 트리거 없는 "자동 수집됨" 카피는 거짓이므로 금지.
